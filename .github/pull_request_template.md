## What and why

<!-- What does this change, and what problem does it solve? Keep it short. -->

Closes #

## Checklist

- [ ] `pnpm lint`, `pnpm check` and `pnpm test` pass
- [ ] New or changed behaviour is covered by a test
- [ ] UI strings go through Paraglide, and `en.json` and `pt-BR.json` both have the new keys
- [ ] Schema changes are a new migration file (no applied migration was edited)
- [ ] The commit messages use a conventional prefix (`feat:`, `fix:`, `test:`, `docs:`, `chore:`)

## Notes for the reviewer

<!-- Anything worth knowing: screenshots, trade-offs, things you are unsure about. -->
