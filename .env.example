# Backups to Google Drive (optional; see "Backups to Google Drive" in README.md).
# The OAuth client id: public, read by the build (the app) and by the Netlify function.
VITE_GOOGLE_CLIENT_ID=
# The OAuth client secret: read only by the Netlify function. Never commit it.
GOOGLE_CLIENT_SECRET=
